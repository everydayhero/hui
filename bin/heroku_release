#!/bin/bash

set -e

git checkout master
git pull
git checkout -b deploy
gulp
git add -f dist/
git commit -m "Deploying to Heroku"
git remote rm heroku || echo 'no worries'
heroku git:remote -a hui-guide
git push heroku -f deploy:master
git checkout master
git branch -D deploy
